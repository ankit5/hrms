{% if children is not empty %} 
<div{{ attributes.addClass(classes) }}>
    <strong class="d-block h5 mb-0 {{ required }} form-item__label">{{ label }}</strong>
    <span class="text-secondary">{{ children }}</span>
</div>
{% endif %}

